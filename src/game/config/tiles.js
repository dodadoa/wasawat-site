/** @deprecated Import from elements.js — kept for backwards compatibility */
export {
  GROUND_ELEMENTS as TILES,
  GROUND_KEYS as TILE_KEYS,
  TILE_SIZE,
  FONT_FAMILY,
  FONT_SIZE,
  TEXT_RESOLUTION,
  THEME,
  getGround,
} from "./elements.js"
